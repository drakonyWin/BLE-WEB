<!DOCTYPE html>
<html>
<head>
  <title>Register</title>
  <style>
    body { font-family: Arial; background: #f0f0f0; display: flex; justify-content: center; align-items: center; height: 100vh; }
    .form-container { background: white; padding: 30px; border-radius: 8px; box-shadow: 0 0 10px #999; }
    input { display: block; width: 100%; padding: 10px; margin: 10px 0; }
    button { background: #970000; color: white; border: none; padding: 10px; width: 100%; cursor: pointer; }
    a { text-align: center; display: block; margin-top: 10px; color: #970000; }
    .error { color: red; text-align: center; }
  </style>
</head>
<body>
  <div class="form-container">
    <h2>Register</h2>
    <% if (message) { %><p class="error"><%= message %></p><% } %>
    <form method="post" action="/register">
      <input type="text" name="username" placeholder="Username" required />
      <input type="password" name="password" placeholder="Password" required />
      <button type="submit">Register</button>
    </form>
    <a href="/login">Already have an account? Login</a>
  </div>
</body>
</html>
